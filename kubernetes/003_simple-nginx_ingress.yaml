apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: simple-nginx
spec:
  rules:
    - host: "54.71.1.45"
      http:
        paths:
          - backend:
              serviceName: simple-nginx
              servicePort: 80
